CUPeru

Requirements

Configuration
-------------

This app requires API credentials for both Twilio and Twitter. These go in the
`config/application.yml` file. There's an `application.yml.sample` file to start
from.

Local installation
------------------

Local setup requires the configuration varaiables to be set.


Deploying to Heroku
